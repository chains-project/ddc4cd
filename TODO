* Add support to release resulting binary ?
* Make report better: add nixpkgs hash used
* Document the code/workflow

Low priority:
* Add version to build dirs to allow different version of the same compiler to be used
